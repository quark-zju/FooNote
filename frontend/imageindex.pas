unit ImageIndex;

{$mode objfpc}{$H+}

interface

uses
  Classes, SysUtils;

const
  IMG_FOLDER = 0;
  IMG_ROOT_LOCAL = 1;
  IMG_ROOT_REMOTE = 2;
  IMG_ADD = 3;
  IMG_SETTINGS = 4;
  IMG_TEXT = 5;
  IMG_TRASH = 6;
  IMG_WARN = 10;
  IMG_ROOT_MEMORY = 12;
  IMG_ROOT_HOME = 13;


implementation

end.

